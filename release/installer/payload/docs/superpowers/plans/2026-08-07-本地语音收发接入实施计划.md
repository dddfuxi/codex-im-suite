# 本地语音收发接入实施计划

> 日期：2026-08-07
> 范围：`codex-im-suite` 开发版，飞书首发
> 版本目标：`0.3.0`

## 目标与边界

- 飞书语音入站由本地 ASR 转写后再交给 Primary Agent；原始音频不冒充当前 Provider 已支持的原生音频输入。
- 收到真实飞书语音后默认用语音回复；`/voice on` 开启会话语音，`/voice off` 在再次开启前硬性禁用所有语音回复；普通文字默认仍返回文字。
- 用户明确要求语音时，Agent 只能通过受限 `cti-final.speech.mode=voice_only` 声明呈现意图，不能指定模型、路径、音色 ID、命令、`file_key` 或平台身份。
- 语音成功时只保留一个飞书原生语音终态；合成、验证、进度卡关闭或上传任一步失败时，只发送一次完整文字。
- 明确唱歌请求必须走独立 ACE-Step 1.5 `SingingHost`；普通 TTS 不得冒充歌声。面板分别选择和试听说话/歌声音色。
- 首期只改飞书。微信继续优先使用平台转写，不改变现有行为。
- 不迁移、不读取、不依赖 `F:\unity\ST4\.cti-audio`；模型、FFmpeg、Python 环境和参考音频不进入 Git、工作区、live skill 或 release 包。

## 架构落点

### Bridge Core

- [x] 声明可选 `SpeechHost`、`cti-speech-transcript/v1` 和受管合成产物回执。
- [x] 在 `prepareForAgent()` 完成后、Primary Agent 启动前处理飞书音频。
- [x] 纯语音使用可靠转写作为有效正文；文字加语音保留附言并加入带来源 ID 的结构化证据。
- [x] 空转写、无声、格式错误、超限或超时不启动 Primary，返回可行动错误。
- [x] 删除纯音频误走“用户发送了一张图片”的默认 Prompt 路线。
- [x] 新增平台无关 `sendLocalAudio()`，飞书实现原生 `file_type=opus + msg_type=audio`。
- [x] 建立 `/voice on|off` 会话策略和受限 `cti-final.speech` 清洗。
- [x] 保证语音与文字只有一个用户可见终态，并在 Store/outbound ref 保留最终文字与真实语音消息 ID。
- [x] 新增受限 `cti-final.singing`、独立 `SingingHost` 和唯一歌声/文字终态，拒绝模型提供 provider、路径、音色 ID、URL、token、命令或平台身份。

### Runtime 与本地 sidecar

- [x] 实现 `CTI_SPEECH_*` / `CTI_SINGING_*` 配置解析、保存和启动覆盖。
- [x] 实现显式配置 → `CTI_HOME\runtime-deps` → PATH 的依赖解析；显式坏路径失败关闭。
- [x] 使用参数数组调用 FFmpeg/ffprobe，完成真实文件头、格式、时长、大小和非符号链接校验。
- [x] 实现只监听 `127.0.0.1`、使用进程临时令牌的版本化语音 sidecar 协议。
- [x] ASR 默认配置为 SenseVoiceSmall GGUF Q8 CPU；TTS 默认配置为 CosyVoice 预设音色，复刻单并发且受性能门禁。
- [x] 实现受管依赖 manifest、安全下载、SHA-256、许可证记录和 Zip Slip 防护；禁止任意命令。SenseVoice 项已固定，CosyVoice/ACE-Step 未完整项继续 blocked。
- [x] 实现音色注册表：授权确认、相对路径、SHA-256、参考转写、来源与许可证。
- [x] 运行状态仅暴露 `ready / optional_missing / blocked / error`、能力和稳定诊断码。
- [x] 实现 ACE-Step 1.5 loopback API 客户端、Bearer token、禁止重定向、benchmark 门禁以及 Ogg/Opus/Hash 产物校验。
- [ ] 补齐 ACE-Step Runtime/模型的固定版本、SHA-256、大小与安全安装目标；当前保持 `blocked / manifest_incomplete`。

### Contracts 与控制面板

- [x] 共享 TypeScript contract、JSON Schema、C# 薄 DTO 和逐字段测试同步。
- [x] 新增“语音”页，显示输入/输出/歌声、渠道、策略、Provider、音色和四态状态。
- [x] 安装/检查、预设音色下载、授权参考音频导入、说话/歌声音色选择与试听只调用 Runtime 受控命令。
- [x] 前端不伪造即时生效，不暴露绝对路径、参考音频、密钥或原始异常。

### 发布与文档

- [x] Suite 与语音相关 workspace package（Contracts、Core、Runtime、Control Panel Web）版本升级为 `0.3.0`；无关 MCP 包保持自身版本线。
- [x] 更新配置样例、README、Runtime 文档、SKILL、架构、开发日志和长期维护规则。
- [x] 复用 release manifest/fork-health 对 Runtime dist、配置样例、SKILL、文档与面板产物的现有指纹覆盖；portable/installer 0.3.0 重建后全部 same。
- [x] 更新仓库内 `packages/bridge-runtime/docs/AI_BRIDGE_CONTEXT.md`；外部 `E:\cli-md\docs\AI_BRIDGE_CONTEXT.md` 不存在且不是本仓库事实源，本轮不创建外部副本。

## 测试矩阵

- [x] 配置往返、依赖解析、固定 SHA、Zip Slip、带空格路径、超时和取消。
- [x] OGG/Opus、WAV、MP3、M4A 文件头与 ffprobe；空文件、格式伪装、20 MB/300 秒门禁和临时文件清理。
- [x] ASR 成功、空结果、无声、超时、错误来源 ID 和伪造 evidence。
- [x] 普通文字、飞书语音、自然语言明确要求、`/voice on` 和 `/voice off` 的优先级。
- [x] TTS 只朗读最终可见正文，不朗读 Markdown 协议或内部诊断。
- [x] 飞书 reply/create 两路均断言 `file_type=opus + msg_type=audio`。
- [x] TTS、进度卡关闭和上传失败均只产生一次完整文字终态。
- [x] 历史回复语音继续绑定原始 `message_id + file_key`。
- [x] ACE-Step 成功只调用 Singing Host、只发一条原生音频；失败只发一次完整文字；夹带 provider/path 的模型协议整段拒绝。
- [x] 说话/歌声试听回执覆盖精确字段、Base64、Ogg 文件头、大小、Hash、Profile 绑定以及浏览器 Blob URL 回收。

## 回归门禁

- [x] `npm run test:contracts`（17/17）
- [x] `npm run test:core`（901/901）
- [x] `npm run test:runtime`（936/936，含 ACE-Step 6/6）
- [x] 控制面板测试（C# 161/161；Web 46/46）
- [x] `npm run test:boundaries`（4/4）
- [x] `npm run check:boundaries`
- [x] `npm run validate:extensions`（29 个声明，0 warning）
- [x] `npm run check:human-docs`
- [x] `npm run build`
- [x] release manifest summary 与 MainPreflight fork-health（portable/installer 全部 same）

## 现场验收（不以开发版测试替代）

- [ ] 安装受管依赖并通过 doctor。
- [ ] RTX 3070：TTS 单并发、无 OOM；10 秒中文 ASR 热态快于实时；约 100 中文字符 TTS 热态不超过 20 秒。
- [ ] RTX 3070：ACE-Step 固定 10 秒试听和完整歌曲生成不 OOM；未通过时歌声继续 blocked。
- [ ] 复刻未过门禁时仅标记复刻 `blocked`，预设音色仍保持可用，禁止静默换音色。
- [ ] 经用户明确授权后同步开发版到 live，并核对开发/live bundle hash。
- [ ] 受控重启，核对新 PID 与飞书长连接。
- [ ] 重启后用真实新语音验证转写与原生语音唯一终态。
- [ ] 验证普通文字只回文字。
- [ ] 人工制造一次 TTS 失败，确认只出现一次完整文字回退。

## 官方来源

- CosyVoice 代码与说明：<https://github.com/FunAudioLLM/CosyVoice>
- CosyVoice-300M-SFT：<https://huggingface.co/FunAudioLLM/CosyVoice-300M-SFT>
- Fun-CosyVoice3-0.5B-2512：<https://huggingface.co/FunAudioLLM/Fun-CosyVoice3-0.5B-2512>
- SenseVoice：<https://github.com/FunAudioLLM/SenseVoice>
- SenseVoiceSmall-GGUF：<https://huggingface.co/FunAudioLLM/SenseVoiceSmall-GGUF>
- ACE-Step 1.5：<https://github.com/ace-step/ACE-Step-1.5>
- ACE-Step 1.5 API：<https://github.com/ace-step/ACE-Step-1.5/blob/main/docs/zh/API.md>
