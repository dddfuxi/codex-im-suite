# 机器人能力治理 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立 Registry 驱动的 Skill 生命周期、Prompt Snapshot、Memory 资产索引和四域控制面板，并保持现有飞书、Skill、MCP、模型、会话和发布能力不变。

**Architecture:** `packages/bridge-core` 只声明平台无关的能力治理策略和宿主契约；`packages/bridge-runtime` 成为 Skill Registry、官方创建/安装适配器、审批、回滚和 Prompt Snapshot 的唯一业务实现；控制面板通过 runtime CLI 读取状态和执行受控动作。迁移按“契约 → Registry → 生命周期 → Prompt/Memory → 面板 → 兼容收口”顺序进行，每一步保留现有入口并通过测试后提交。

**Tech Stack:** TypeScript、Node.js 20、Node test runner、tsx、React 19、Vite、.NET 9 WinForms/WebView2、xUnit、PowerShell、Mermaid。

---

## 文件结构锁定

新增文件职责如下：

- `packages/bridge-runtime/src/skill-registry.ts`：扫描、合并、持久化 Skill 资产状态，不执行安装。
- `packages/bridge-runtime/src/skill-source-policy.ts`：根据来源、风险和变化类型生成自治/审批决策。
- `packages/bridge-runtime/src/official-skill-tools.ts`：只负责安全调用官方 `skill-creator`、`skill-installer` 脚本。
- `packages/bridge-runtime/src/skill-lifecycle.ts`：编排草稿、验证、审批、安装、启停、更新和回滚。
- `packages/bridge-runtime/src/skill-lifecycle-cli.ts`：给控制面板、脚本和人工诊断使用的 JSON CLI。
- `packages/bridge-core/src/lib/bridge/prompt-composer.ts`：生成带来源标签的 Prompt Section，并保持最终拼装顺序。
- `packages/bridge-core/src/lib/bridge/prompt-snapshot.ts`：Prompt Snapshot 纯函数、脱敏、哈希和截断元数据。
- `packages/bridge-runtime/src/prompt-snapshot-store.ts`：短期 Snapshot 原子持久化和保留策略。
- `apps/control-panel/SkillLifecycleGateway.cs`：控制面板调用 runtime CLI 的薄网关，不包含 Skill 业务判断。
- `apps/control-panel/web/src/panel-navigation.ts`：四域导航、旧页面兼容映射和页面元数据。
- `apps/control-panel/web/src/skill-view-model.ts`：Skill 页面纯筛选和状态映射。
- `apps/control-panel/web/src/pages/ArchitecturePage.tsx`：机器人八层架构只读视图。
- `apps/control-panel/web/src/pages/PromptPage.tsx`：Prompt Snapshot 只读视图。
- `apps/control-panel/web/src/pages/SkillsPage.tsx`：已安装、草稿、目录、审批队列。

现有 `apps/control-panel/web/src/main.tsx` 保留应用壳、状态订阅和命令发送；逐步移出新页面，不在一次提交里重写全部面板。

---

### Task 1：建立 Skill 治理契约与自治策略入口

本任务同时锁定“只有现有能力不足且当前任务确实需要时才搜索外部 Skill”，不能把目录搜索当作普通推荐或面板填充行为。

**Files:**
- Modify: `packages/bridge-core/src/lib/bridge/agent-architecture.ts`
- Modify: `packages/bridge-core/src/lib/bridge/host.ts`
- Modify: `packages/bridge-core/src/__tests__/unit/bridge-agent-architecture.test.ts`

- [x] **Step 1：先写失败测试，覆盖已确认的自治矩阵**

在 `bridge-agent-architecture.test.ts` 增加：

```ts
it('decides skill autonomy from source and risk instead of names', () => {
  assert.equal(decideSkillLifecycleAction({ installed: true, sourceClass: 'installed', risk: 'low', changeKind: 'none' }), 'use');
  assert.equal(decideSkillLifecycleAction({ installed: false, sourceClass: 'official_curated', risk: 'low', changeKind: 'install' }), 'confirm_user');
  assert.equal(decideSkillLifecycleAction({ installed: false, sourceClass: 'whitelist', risk: 'low', changeKind: 'install' }), 'auto_install');
  assert.equal(decideSkillLifecycleAction({ installed: false, sourceClass: 'self_created', risk: 'low', changeKind: 'install' }), 'confirm_user');
  assert.equal(decideSkillLifecycleAction({ installed: false, sourceClass: 'third_party', risk: 'medium', changeKind: 'install' }), 'confirm_owner');
  assert.equal(decideSkillLifecycleAction({ installed: true, sourceClass: 'whitelist', risk: 'high', changeKind: 'permissions' }), 'confirm_owner');
});

it('searches external skills only for a real unmet capability requirement', () => {
  assert.equal(shouldSearchSkillCatalog({ taskRequiresCapability: false, installedCandidateCount: 0 }), false);
  assert.equal(shouldSearchSkillCatalog({ taskRequiresCapability: true, installedCandidateCount: 1 }), false);
  assert.equal(shouldSearchSkillCatalog({ taskRequiresCapability: true, installedCandidateCount: 0 }), true);
});
```

- [x] **Step 2：运行测试并确认因接口不存在而失败**

Run:

```powershell
node --test --import tsx packages/bridge-core/src/__tests__/unit/bridge-agent-architecture.test.ts
```

Expected: FAIL，提示 `decideSkillLifecycleAction` 未导出。

- [x] **Step 3：实现最小平台无关契约**

在 `agent-architecture.ts` 增加并导出：

```ts
export type SkillSourceClass = 'installed' | 'official_curated' | 'whitelist' | 'self_created' | 'third_party' | 'unknown';
export type SkillRiskLevel = 'low' | 'medium' | 'high';
export type SkillChangeKind = 'none' | 'docs' | 'compatibility' | 'install' | 'enable' | 'trigger' | 'permissions' | 'scripts' | 'write_scope';
export type SkillLifecycleAction = 'use' | 'auto_update' | 'auto_install' | 'confirm_user' | 'confirm_owner' | 'quarantine';

export interface SkillLifecyclePolicyInput {
  installed: boolean;
  sourceClass: SkillSourceClass;
  risk: SkillRiskLevel;
  changeKind: SkillChangeKind;
}

export interface SkillCapabilityGapInput {
  taskRequiresCapability: boolean;
  installedCandidateCount: number;
}

export function shouldSearchSkillCatalog(input: SkillCapabilityGapInput): boolean {
  return input.taskRequiresCapability && input.installedCandidateCount === 0;
}

export function decideSkillLifecycleAction(input: SkillLifecyclePolicyInput): SkillLifecycleAction {
  if (input.risk === 'high' || ['permissions', 'scripts', 'write_scope'].includes(input.changeKind)) return 'confirm_owner';
  if (input.sourceClass === 'third_party' || input.sourceClass === 'unknown') return 'confirm_owner';
  if (input.installed && input.changeKind === 'none') return 'use';
  if (input.installed && ['docs', 'compatibility'].includes(input.changeKind)) return 'auto_update';
  if (input.sourceClass === 'whitelist' && input.risk === 'low') return 'auto_install';
  if (input.sourceClass === 'official_curated' || input.sourceClass === 'self_created') return 'confirm_user';
  return 'quarantine';
}
```

在 `host.ts` 增加 Registry、审批和 Snapshot 宿主类型；类型只描述数据，不引入 Node 文件系统依赖。

- [x] **Step 4：运行核心测试**

Run:

```powershell
npm --workspace packages/bridge-core test
```

Expected: `386` 个以上测试全部 PASS，新增自治策略测试 PASS。

- [x] **Step 5：提交契约**

```powershell
git add packages/bridge-core/src/lib/bridge/agent-architecture.ts packages/bridge-core/src/lib/bridge/host.ts packages/bridge-core/src/__tests__/unit/bridge-agent-architecture.test.ts
git commit -m "add skill governance policy contracts"
```

---

### Task 2：实现 Skill Registry 扫描、迁移和原子持久化

**Files:**
- Create: `packages/bridge-runtime/src/skill-registry.ts`
- Create: `packages/bridge-runtime/src/__tests__/skill-registry.test.ts`
- Modify: `packages/bridge-runtime/src/config.ts`

- [x] **Step 1：写失败测试，证明扫描不会移动现有 Skill**

```ts
it('merges manifests, installed skills and drafts without rewriting SKILL.md', () => {
  const registry = createSkillRegistry({ ctiHome, codexHome, suiteRoot, now: () => fixedNow });
  const snapshot = registry.refresh();
  assert.equal(snapshot.protocol, 'cti-skill-registry/v1');
  assert.equal(snapshot.items.find((item) => item.id === 'installed-skill')?.state, 'enabled');
  assert.equal(snapshot.items.find((item) => item.id === 'draft-skill')?.state, 'draft');
  assert.equal(fs.readFileSync(installedSkillPath, 'utf8'), installedSkillBefore);
});
```

再覆盖：损坏 Registry 从备份恢复、重复 ID 按“正式安装 > 草稿 > manifest 声明”归并、所有路径保持绝对路径。

- [x] **Step 2：运行目标测试确认 RED**

```powershell
node --test --test-concurrency=1 --import tsx --test-timeout=15000 packages/bridge-runtime/src/__tests__/skill-registry.test.ts
```

Expected: FAIL，提示模块不存在。

- [x] **Step 3：实现 Registry 数据结构和扫描器**

核心类型：

```ts
export type SkillRegistryState = 'discovered' | 'draft' | 'validated' | 'approval_pending' | 'installed' | 'enabled' | 'disabled' | 'quarantined';

export interface SkillRegistryItem {
  id: string;
  displayName: string;
  version?: string;
  sourceClass: SkillSourceClass;
  source?: string;
  path?: string;
  contentHash?: string;
  state: SkillRegistryState;
  risk: SkillRiskLevel;
  enabled: boolean;
  relatedProjects?: string[];
  validation?: { ok: boolean; checkedAt: string; summary: string };
  approval?: { required: 'none' | 'user' | 'owner'; nonce?: string; expiresAt?: string };
  failureSummary?: string;
  rollbackPath?: string;
  updatedAt: string;
}

export interface SkillRegistrySnapshot {
  protocol: 'cti-skill-registry/v1';
  generatedAt: string;
  items: SkillRegistryItem[];
}
```

Registry 路径固定由 `CTI_HOME` 推导为 `data/skill-registry.json`；草稿目录为 `extensions/drafts/skills`。写入复用原子临时文件、rename、`.bak` 回退，不扫描 `.codepilot-uploads`、release 或 live skill 源码副本。

- [x] **Step 4：运行 runtime 测试和类型检查**

```powershell
npm --workspace packages/bridge-runtime run typecheck
npm --workspace packages/bridge-runtime test
```

Expected: 全部 PASS，测试临时目录中没有被移动或改写的 `SKILL.md`。

- [x] **Step 5：提交 Registry**

```powershell
git add packages/bridge-runtime/src/skill-registry.ts packages/bridge-runtime/src/__tests__/skill-registry.test.ts packages/bridge-runtime/src/config.ts
git commit -m "add skill registry storage"
```

---

### Task 3：接入官方 Skill 创建、校验和安装脚本

**Files:**
- Create: `packages/bridge-runtime/src/official-skill-tools.ts`
- Create: `packages/bridge-runtime/src/skill-source-policy.ts`
- Create: `packages/bridge-runtime/src/skill-lifecycle.ts`
- Create: `packages/bridge-runtime/src/__tests__/official-skill-tools.test.ts`
- Create: `packages/bridge-runtime/src/__tests__/skill-lifecycle.test.ts`

- [x] **Step 1：写失败测试，固定官方脚本调用参数和事务边界**

```ts
it('uses official creator and validator scripts without shell interpolation', async () => {
  const calls: ProcessCall[] = [];
  const tools = createOfficialSkillTools({ codexHome, pythonExe: 'python', run: async (call) => { calls.push(call); return okResult; } });
  await tools.createDraft({ name: 'asset-cleaner', draftRoot, displayName: 'Asset Cleaner', description: 'Clean assets', defaultPrompt: 'Clean this asset' });
  await tools.validate(path.join(draftRoot, 'asset-cleaner'));
  assert.equal(calls[0].file, 'python');
  assert.deepEqual(calls[0].args.slice(0, 3), [initSkillScript, 'asset-cleaner', '--path']);
  assert.equal(calls[1].args[0], quickValidateScript);
});
```

生命周期测试覆盖：官方精选生成 `confirm_user`；白名单低风险自动安装；第三方 URL 生成 `confirm_owner`；安装失败恢复旧目录。

再覆盖所有创建、验证、审批、安装、启停和回滚动作追加脱敏审计记录；审计中不得出现 token、私密配置正文或完整 `SKILL.md`。

- [x] **Step 2：运行测试确认 RED**

```powershell
node --test --test-concurrency=1 --import tsx packages/bridge-runtime/src/__tests__/official-skill-tools.test.ts packages/bridge-runtime/src/__tests__/skill-lifecycle.test.ts
```

Expected: FAIL，模块不存在。

- [x] **Step 3：实现官方适配器和生命周期服务**

官方脚本位置必须从 `CODEX_HOME` 推导：

```ts
const creatorRoot = path.join(codexHome, 'skills', '.system', 'skill-creator');
const installerRoot = path.join(codexHome, 'skills', '.system', 'skill-installer');
```

只允许 `spawn`/`execFile` 参数数组，不拼接 shell 命令。调用入口：

- `skill-creator/scripts/init_skill.py`
- `skill-creator/scripts/quick_validate.py`
- `skill-installer/scripts/list-skills.py --format json`
- `skill-installer/scripts/install-skill-from-github.py`

`SkillLifecycleService` 公开：

```ts
export interface CreateSkillDraftInput {
  name: string;
  displayName: string;
  description: string;
  defaultPrompt: string;
  actor: ExtensionActionActor;
}

export interface PrepareSkillInstallInput {
  id: string;
  sourceClass: SkillSourceClass;
  source: string;
  actor: ExtensionActionActor;
}

export interface SkillApprovalRecord {
  nonce: string;
  skillId: string;
  requiredRole: 'user' | 'owner';
  actor: Pick<ExtensionActionActor, 'channelType' | 'chatId' | 'userId'>;
  expiresAt: string;
}

export interface SkillLifecycleService {
  snapshot(): SkillRegistrySnapshot;
  search(query: string): Promise<SkillRegistryItem[]>;
  createDraft(input: CreateSkillDraftInput): Promise<SkillRegistryItem>;
  validate(id: string): Promise<SkillRegistryItem>;
  prepareInstall(input: PrepareSkillInstallInput): Promise<SkillApprovalRecord | SkillRegistryItem>;
  confirmInstall(nonce: string, actor: ExtensionActionActor): Promise<SkillRegistryItem>;
  setEnabled(id: string, enabled: boolean, actor: ExtensionActionActor): Promise<SkillRegistryItem>;
  rollback(id: string, actor: ExtensionActionActor): Promise<SkillRegistryItem>;
}
```

安装先写 `CTI_HOME/extensions/staging/skills`，校验成功后再替换 `CODEX_HOME/skills/<id>`；旧版本移动到 `CTI_HOME/extensions/backups/skills/<id>/<timestamp>`。确认 nonce 必须绑定原用户、原会话和过期时间。

生命周期审计追加到 `CTI_HOME/data/skill-lifecycle-audit.jsonl`，每条只记录动作、Skill ID、来源等级、风险、审批角色、actor 的受控身份字段、结果、时间和失败摘要；写入使用 UTF-8，失败不得伪造成功状态。

- [x] **Step 4：运行 runtime 全量测试**

```powershell
npm --workspace packages/bridge-runtime test
npm --workspace packages/bridge-runtime run build
```

Expected: 全部 PASS；构建输出仍包含 daemon 和 memory optimizer，且无 shell 注入字符串。

- [x] **Step 5：提交官方适配器**

```powershell
git add packages/bridge-runtime/src/official-skill-tools.ts packages/bridge-runtime/src/skill-source-policy.ts packages/bridge-runtime/src/skill-lifecycle.ts packages/bridge-runtime/src/__tests__/official-skill-tools.test.ts packages/bridge-runtime/src/__tests__/skill-lifecycle.test.ts
git commit -m "add official skill lifecycle adapters"
```

---

### Task 4：让飞书入口和 CLI 共用同一生命周期服务

**Files:**
- Create: `packages/bridge-runtime/src/skill-lifecycle-cli.ts`
- Create: `packages/bridge-runtime/src/__tests__/skill-lifecycle-cli.test.ts`
- Modify: `packages/bridge-runtime/src/extension-catalog-host.ts`
- Modify: `packages/bridge-runtime/src/__tests__/extension-catalog-host.test.ts`
- Modify: `packages/bridge-runtime/src/main.ts`
- Modify: `packages/bridge-runtime/scripts/build.js`

- [ ] **Step 1：写失败测试，证明 Skill 不再调用面板安装命令**

```ts
it('routes skill installation through lifecycle while leaving non-skill extensions on the legacy adapter', async () => {
  const host = createExtensionCatalogHost({ lifecycle, request });
  await host.prepareInstallAction({ item: skillItem, actor });
  assert.equal(lifecycleCalls.length, 1);
  assert.equal(controlApiCalls.length, 0);
  await host.prepareInstallAction({ item: mcpItem, actor });
  assert.equal(controlApiCalls.length, 0);
  await host.confirmInstallAction('mcp-confirmation', actor);
  assert.equal(controlApiCalls[0].command, 'extension.remote.install');
});
```

CLI 测试通过注入 service factory，覆盖 `snapshot`、`search`、`create-draft`、`validate`、`prepare-install`、`confirm-install`、`enable`、`disable`、`rollback` 的 JSON 输出和错误退出码。

- [ ] **Step 2：运行目标测试确认 RED**

```powershell
node --test --test-concurrency=1 --import tsx packages/bridge-runtime/src/__tests__/extension-catalog-host.test.ts packages/bridge-runtime/src/__tests__/skill-lifecycle-cli.test.ts
```

Expected: FAIL，host 尚无 lifecycle 依赖，CLI 不存在。

- [ ] **Step 3：实现路由和 CLI**

`createExtensionCatalogHost` 增加 `lifecycle` 选项；当 `item.type === 'skill'` 时只调用 lifecycle，其他类型保持现有 Control API 兼容路径。`main.ts` 创建一次 Registry/Lifecycle 实例并传入 bridge context，禁止每次请求新建不同状态实例。

先把两个现有构建入口重复使用的 `external` 和 `banner` 提取到文件顶部，再增加第三个 esbuild entry。共享常量必须在三个 `esbuild.build()` 调用之前定义：

```js
const sharedExternals = [
  '@anthropic-ai/claude-agent-sdk',
  '@openai/codex-sdk',
  'bufferutil', 'utf-8-validate', 'zlib-sync', 'erlpack',
  'fs', 'path', 'os', 'crypto', 'http', 'https', 'net', 'tls',
  'stream', 'events', 'url', 'util', 'child_process', 'worker_threads',
  'node:*',
];

const sharedBanner = {
  js: "import { createRequire } from 'module'; const require = createRequire(import.meta.url);",
};

// daemon.mjs 和 memory-optimizer-cli.mjs 的现有构建配置保持不变，
// 只把各自内联的 external/banner 改成下面两个共享引用。
// external: sharedExternals,
// banner: sharedBanner,

await esbuild.build({
  entryPoints: ['src/skill-lifecycle-cli.ts'],
  bundle: true,
  platform: 'node',
  format: 'esm',
  target: 'node20',
  outfile: 'dist/skill-lifecycle-cli.mjs',
  external: sharedExternals,
  banner: sharedBanner,
});
```

同步更新构建日志为三个产物。

- [ ] **Step 4：运行核心、runtime 测试和构建**

```powershell
npm --workspace packages/bridge-core test
npm --workspace packages/bridge-runtime test
npm --workspace packages/bridge-runtime run build
```

Expected: 全部 PASS；`packages/bridge-runtime/dist/skill-lifecycle-cli.mjs` 存在。

- [ ] **Step 5：提交统一入口**

```powershell
git add packages/bridge-runtime/src/skill-lifecycle-cli.ts packages/bridge-runtime/src/__tests__/skill-lifecycle-cli.test.ts packages/bridge-runtime/src/extension-catalog-host.ts packages/bridge-runtime/src/__tests__/extension-catalog-host.test.ts packages/bridge-runtime/src/main.ts packages/bridge-runtime/scripts/build.js
git commit -m "route skill actions through lifecycle service"
```

---

### Task 5：拆分 Prompt Composer 并持久化只读 Snapshot

**Files:**
- Create: `packages/bridge-core/src/lib/bridge/prompt-composer.ts`
- Create: `packages/bridge-core/src/lib/bridge/prompt-snapshot.ts`
- Create: `packages/bridge-core/src/__tests__/unit/prompt-composer.test.ts`
- Create: `packages/bridge-core/src/__tests__/unit/prompt-snapshot.test.ts`
- Modify: `packages/bridge-core/src/lib/bridge/conversation-engine.ts`
- Modify: `packages/bridge-core/src/lib/bridge/host.ts`
- Create: `packages/bridge-runtime/src/prompt-snapshot-store.ts`
- Create: `packages/bridge-runtime/src/__tests__/prompt-snapshot-store.test.ts`
- Modify: `packages/bridge-runtime/src/store.ts`

- [ ] **Step 1：写失败测试，固定分类、顺序、脱敏和保留策略**

```ts
it('composes prompt sections in stable priority order', () => {
  const result = composeBridgePrompt({ identity: 'identity', base: 'base', policy: 'policy', memory: 'memory', skills: 'skills', style: 'style', protocol: 'protocol' });
  assert.deepEqual(result.sections.map((item) => item.kind), ['identity', 'base', 'policy', 'skills', 'memory', 'style', 'protocol']);
  assert.equal(result.text, result.sections.map((item) => item.content).join('\n\n'));
});

it('redacts secrets and records truncation without storing raw tokens', () => {
  const snapshot = createPromptSnapshot({ sessionId: 's1', sections: [{ id: 'policy', kind: 'policy', source: 'test', content: 'token=secret-value' }], maxSectionChars: 12 });
  assert.doesNotMatch(JSON.stringify(snapshot), /secret-value/);
  assert.equal(snapshot.sections[0].truncated, true);
  assert.match(snapshot.sections[0].hash, /^[a-f0-9]{64}$/);
});
```

Store 测试覆盖时间和数量清理、损坏文件回退、写入失败不抛到主聊天链。

- [ ] **Step 2：运行目标测试确认 RED**

```powershell
node --test --import tsx packages/bridge-core/src/__tests__/unit/prompt-composer.test.ts packages/bridge-core/src/__tests__/unit/prompt-snapshot.test.ts
node --test --test-concurrency=1 --import tsx packages/bridge-runtime/src/__tests__/prompt-snapshot-store.test.ts
```

Expected: FAIL，模块不存在。

- [ ] **Step 3：实现结构化拼装和 best-effort 记录**

`prompt-composer.ts` 将现有 `buildBridgeScopedSystemPrompt` 拆为 `PromptSection[]`，保持最终文本顺序和现有规则原文不变。Section kind 使用：

```ts
export type PromptSectionKind = 'identity' | 'base' | 'policy' | 'skills' | 'memory' | 'style' | 'protocol' | 'priority_context' | 'execution';

export interface PromptSection {
  id: string;
  kind: PromptSectionKind;
  source: string;
  priority: number;
  content: string;
}

export interface ComposedBridgePrompt {
  sections: PromptSection[];
  text: string;
}

export interface PromptSnapshotSection {
  id: string;
  kind: PromptSectionKind;
  source: string;
  priority: number;
  charCount: number;
  hash: string;
  injected: boolean;
  truncated: boolean;
  truncationReason?: 'section_limit' | 'snapshot_limit' | 'redacted';
  content: string;
}
```

`conversation-engine.ts` 在调用 `llm.streamChat` 前生成 Snapshot，并通过可选 `store.recordPromptSnapshot` 写入；写入异常只记录警告，不能阻断 stream。

Snapshot 路径为 `CTI_HOME/runtime/prompt-snapshots.json`，默认保留数量和天数从配置读取；未配置时采用代码常量，但面板必须显示当前策略。

- [ ] **Step 4：证明最终 Prompt 行为未改变**

```powershell
npm --workspace packages/bridge-core test
npm --workspace packages/bridge-runtime test
```

Expected: 全部 PASS；现有 conversation-engine prompt 断言不回归；新增 Snapshot 测试 PASS。

- [ ] **Step 5：提交 Prompt Snapshot**

```powershell
git add packages/bridge-core/src/lib/bridge/prompt-composer.ts packages/bridge-core/src/lib/bridge/prompt-snapshot.ts packages/bridge-core/src/__tests__/unit/prompt-composer.test.ts packages/bridge-core/src/__tests__/unit/prompt-snapshot.test.ts packages/bridge-core/src/lib/bridge/conversation-engine.ts packages/bridge-core/src/lib/bridge/host.ts packages/bridge-runtime/src/prompt-snapshot-store.ts packages/bridge-runtime/src/__tests__/prompt-snapshot-store.test.ts packages/bridge-runtime/src/store.ts
git commit -m "add observable prompt snapshots"
```

---

### Task 6：控制面板改用 runtime Skill CLI

**Files:**
- Create: `apps/control-panel/SkillLifecycleGateway.cs`
- Create: `apps/control-panel/ControlPanel.Tests/SkillLifecycleGatewayTests.cs`
- Modify: `apps/control-panel/Program.cs`

- [ ] **Step 1：写失败 xUnit 测试，固定薄网关职责**

```csharp
[Fact]
public async Task SnapshotAsync_InvokesRuntimeCliAndParsesJson()
{
    var calls = new List<IReadOnlyList<string>>();
    var gateway = new SkillLifecycleGateway(_ => Task.FromResult(new SkillCliResult(0, "{\"protocol\":\"cti-skill-registry/v1\",\"items\":[]}", "")), args => calls.Add(args));
    var snapshot = await gateway.RunAsync("snapshot", new Dictionary<string, string>());
    Assert.Equal("cti-skill-registry/v1", snapshot.RootElement.GetProperty("protocol").GetString());
    Assert.Equal("snapshot", calls[0][0]);
}
```

再覆盖非零退出码、无效 JSON、CLI 缺失和参数不经 shell 拼接。

- [ ] **Step 2：运行测试确认 RED**

```powershell
dotnet test apps/control-panel/ControlPanel.Tests/CodexImSuite.ControlPanel.Tests.csproj --filter FullyQualifiedName~SkillLifecycleGatewayTests
```

Expected: FAIL，类型不存在。

- [ ] **Step 3：实现 gateway 和新命令**

Gateway 只定位以下 CLI 并传递参数：

- 开发版：`packages/bridge-runtime/dist/skill-lifecycle-cli.mjs`
- live：`<skillDir>/dist/skill-lifecycle-cli.mjs`

在 `ExecuteWebCommandAsync` 增加：

```csharp
case "skill.registry.snapshot":
    return await GetSkillLifecycleGateway().RunAsync("snapshot", []);
case "skill.catalog.search":
    return await GetSkillLifecycleGateway().RunAsync("search", ReadStringMap(payload));
case "skill.draft.create":
    return await GetSkillLifecycleGateway().RunAsync("create-draft", ReadStringMap(payload));
case "skill.lifecycle.validate":
    return await GetSkillLifecycleGateway().RunAsync("validate", ReadStringMap(payload));
case "skill.lifecycle.prepareInstall":
    return await GetSkillLifecycleGateway().RunAsync("prepare-install", ReadStringMap(payload));
case "skill.lifecycle.confirmInstall":
    return await GetSkillLifecycleGateway().RunAsync("confirm-install", ReadStringMap(payload));
case "skill.lifecycle.enable":
    return await GetSkillLifecycleGateway().RunAsync("enable", ReadStringMap(payload));
case "skill.lifecycle.disable":
    return await GetSkillLifecycleGateway().RunAsync("disable", ReadStringMap(payload));
case "skill.lifecycle.rollback":
    return await GetSkillLifecycleGateway().RunAsync("rollback", ReadStringMap(payload));
```

`BuildWebStateAsync` 增加 `skillGovernance` 和 `promptSnapshots`；面板只读取 JSON，不在 C# 中重算风险或安装状态。旧 `extension.*` Skill 操作在兼容期转发到上述命令，MCP、Plugin、模型路径保持原行为。

- [ ] **Step 4：运行控制面板测试和构建**

```powershell
dotnet test apps/control-panel/ControlPanel.Tests/CodexImSuite.ControlPanel.Tests.csproj
dotnet build apps/control-panel/CodexImSuite.ControlPanel.csproj -c Release
```

Expected: 全部 PASS，Release 构建成功。

- [ ] **Step 5：提交 gateway**

```powershell
git add apps/control-panel/SkillLifecycleGateway.cs apps/control-panel/ControlPanel.Tests/SkillLifecycleGatewayTests.cs apps/control-panel/Program.cs
git commit -m "connect panel to skill lifecycle cli"
```

---

### Task 7：建立前端纯函数测试和四域导航

**Files:**
- Modify: `apps/control-panel/web/package.json`
- Modify: `apps/control-panel/web/package-lock.json`
- Create: `apps/control-panel/web/src/panel-navigation.ts`
- Create: `apps/control-panel/web/src/panel-navigation.test.ts`
- Modify: `apps/control-panel/web/src/main.tsx`
- Modify: `apps/control-panel/web/src/styles.css`

- [ ] **Step 1：添加最小测试入口并写失败测试**

只增加已在仓库使用的 `tsx`，不引入新的 UI 框架：

```json
{
  "scripts": {
    "test": "node --test --import tsx src/*.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
```

导航测试：

```ts
it('groups pages into the four confirmed domains and redirects legacy ids', () => {
  assert.deepEqual(panelNavigation.map((group) => group.id), ['run', 'robot', 'capability', 'governance']);
  assert.equal(resolvePageId('extensions'), 'skills');
  assert.equal(resolvePageId('nodes'), 'services');
  assert.equal(resolvePageId('executors'), 'services');
  assert.equal(resolvePageId('memory'), 'memory');
});
```

- [ ] **Step 2：运行测试确认 RED**

```powershell
npm --workspace apps/control-panel/web test
```

Expected: FAIL，`panel-navigation.ts` 不存在。

- [ ] **Step 3：实现四域导航 schema 和兼容映射**

```ts
export const panelNavigation = [
  { id: 'run', label: '运行', pages: ['overview', 'services', 'sessions'] },
  { id: 'robot', label: '机器人', pages: ['architecture', 'prompts', 'memory'] },
  { id: 'capability', label: '能力', pages: ['skills', 'mcp', 'modelsPlugins'] },
  { id: 'governance', label: '治理', pages: ['permissions', 'release', 'logs', 'settings'] },
] as const;

const legacyPageMap = { extensions: 'skills', nodes: 'services', executors: 'services' } as const;
```

`main.tsx` 侧栏按 group 渲染标题和页面；URL hash 继续兼容旧 ID。服务页用内部 tab 保留节点和执行器，不删除原功能。

- [ ] **Step 4：运行前端测试和构建**

```powershell
npm --workspace apps/control-panel/web test
npm --workspace apps/control-panel/web run build
```

Expected: 导航测试 PASS，Vite build 成功。

- [ ] **Step 5：提交导航**

```powershell
git add apps/control-panel/web/package.json apps/control-panel/web/package-lock.json apps/control-panel/web/src/panel-navigation.ts apps/control-panel/web/src/panel-navigation.test.ts apps/control-panel/web/src/main.tsx apps/control-panel/web/src/styles.css
git commit -m "group panel navigation into four domains"
```

---

### Task 8：增加机器人架构页和 Prompt Snapshot 页

**Files:**
- Create: `apps/control-panel/web/src/pages/ArchitecturePage.tsx`
- Create: `apps/control-panel/web/src/pages/PromptPage.tsx`
- Create: `apps/control-panel/web/src/prompt-view-model.ts`
- Create: `apps/control-panel/web/src/prompt-view-model.test.ts`
- Modify: `apps/control-panel/web/src/main.tsx`
- Modify: `apps/control-panel/web/src/styles.css`

- [ ] **Step 1：写失败测试，固定来源排序和截断提示**

```ts
it('orders prompt sections and exposes truncation risk', () => {
  const rows = buildPromptSectionRows(snapshot);
  assert.deepEqual(rows.map((row) => row.kind), ['identity', 'policy', 'skills', 'memory', 'style', 'protocol']);
  assert.equal(rows.find((row) => row.kind === 'memory')?.warning, '已截断');
});
```

- [ ] **Step 2：运行测试确认 RED**

```powershell
npm --workspace apps/control-panel/web test
```

Expected: FAIL，view model 不存在。

- [ ] **Step 3：实现只读页面**

`ArchitecturePage` 读取现有系统蓝图和八层 Registry，只展示职责、拥有项和排除项。`PromptPage` 展示最近 Snapshot、session、时间、总字符数、来源、优先级、哈希短值、注入/截断状态和脱敏后的内容。

页面只提供：刷新、复制脱敏内容、打开真实来源。禁止直接编辑合成 Prompt；需要修改时调用“生成审批草稿”命令，不在本任务实现在线编辑器。

- [ ] **Step 4：运行测试和 build**

```powershell
npm --workspace apps/control-panel/web test
npm --workspace apps/control-panel/web run build
```

Expected: 全部 PASS，两个新页面可编译。

- [ ] **Step 5：提交机器人页面**

```powershell
git add apps/control-panel/web/src/pages/ArchitecturePage.tsx apps/control-panel/web/src/pages/PromptPage.tsx apps/control-panel/web/src/prompt-view-model.ts apps/control-panel/web/src/prompt-view-model.test.ts apps/control-panel/web/src/main.tsx apps/control-panel/web/src/styles.css
git commit -m "add robot architecture and prompt views"
```

---

### Task 9：拆分 Skills、MCP、模型与插件页面

**Files:**
- Create: `apps/control-panel/web/src/skill-view-model.ts`
- Create: `apps/control-panel/web/src/skill-view-model.test.ts`
- Create: `apps/control-panel/web/src/pages/SkillsPage.tsx`
- Create: `apps/control-panel/web/src/pages/McpPage.tsx`
- Create: `apps/control-panel/web/src/pages/ModelsPluginsPage.tsx`
- Modify: `apps/control-panel/web/src/main.tsx`
- Modify: `apps/control-panel/web/src/styles.css`

- [ ] **Step 1：写失败测试，固定四类 Skill 视图和风险文案**

```ts
it('partitions registry items into installed, drafts, catalog and approvals', () => {
  const view = buildSkillWorkspace(registrySnapshot);
  assert.deepEqual(view.installed.map((item) => item.id), ['installed']);
  assert.deepEqual(view.drafts.map((item) => item.id), ['draft']);
  assert.deepEqual(view.catalog.map((item) => item.id), ['curated']);
  assert.deepEqual(view.approvals.map((item) => item.id), ['pending']);
});
```

再断言官方精选显示“询问后安装”，白名单低风险显示“可自动处理”，高风险显示“Owner 审批”。

- [ ] **Step 2：运行测试确认 RED**

```powershell
npm --workspace apps/control-panel/web test
```

Expected: FAIL，view model 不存在。

- [ ] **Step 3：实现三个能力页面并保留现有功能**

- `SkillsPage`：只调用 `skill.*` 命令，提供已安装、草稿、能力目录、审批队列。
- `McpPage`：复用现有 runtime units、注册、检查、启停和 manifest 详情。
- `ModelsPluginsPage`：保留 Ollama 模型安装任务、Plugin 记录和在线目录筛选。

删除 `ExtensionsPage` 中 Skill 的直接 `extension.remote.install` 分支；MCP、模型、Plugin 原命令保持兼容。未知远程 Skill URL 只能生成 Owner 审批，不允许 `allowUntrusted=true` 直接安装。

- [ ] **Step 4：运行测试、构建和手工路由检查**

```powershell
npm --workspace apps/control-panel/web test
npm --workspace apps/control-panel/web run build
```

Expected: PASS；旧 `#extensions` 自动进入 `#skills`；三页数据不重复。

- [ ] **Step 5：提交能力页面**

```powershell
git add apps/control-panel/web/src/skill-view-model.ts apps/control-panel/web/src/skill-view-model.test.ts apps/control-panel/web/src/pages/SkillsPage.tsx apps/control-panel/web/src/pages/McpPage.tsx apps/control-panel/web/src/pages/ModelsPluginsPage.tsx apps/control-panel/web/src/main.tsx apps/control-panel/web/src/styles.css
git commit -m "separate skill mcp model and plugin views"
```

---

### Task 10：把 Memory 页收口为索引并迁移原有维护功能

**Files:**
- Modify: `apps/control-panel/web/src/main.tsx`
- Modify: `apps/control-panel/web/src/styles.css`
- Modify: `apps/control-panel/Program.cs`
- Modify: `apps/control-panel/ControlPanel.Tests/MemoryArtifactStoreTests.cs`

- [ ] **Step 1：写失败测试，证明 Skill 索引不复制正文**

```csharp
[Fact]
public void BuildSkillMemoryIndex_StoresReferencesWithoutSkillBody()
{
    var index = MemoryArtifactStore.BuildSkillAssetIndex(registrySnapshot);
    var item = Assert.Single(index.Items);
    Assert.Equal("asset-cleaner", item.Id);
    Assert.Equal(skillPath, item.SourcePath);
    Assert.Null(item.SkillBody);
}
```

- [ ] **Step 2：运行测试确认 RED**

```powershell
dotnet test apps/control-panel/ControlPanel.Tests/CodexImSuite.ControlPanel.Tests.csproj --filter FullyQualifiedName~MemoryArtifactStoreTests
```

Expected: FAIL，索引 API 不存在。

- [ ] **Step 3：整理 Memory、提醒和维护入口**

Memory 页只保留：知识/事实索引、会话/历史索引、Skill 资产引用、来源打开、关系视图和检索。Skill 行只显示 Registry 元数据和真实路径，不读取完整 `SKILL.md` 到面板状态。

功能迁移：

- 直接提醒列表和完成动作移到“运行 → 会话”的提醒 tab。
- 记忆优化、归档恢复和定期整理移到“治理 → 设置”的数据治理区。
- 表情包资产继续作为 Memory 索引类型，但编辑动作保留现有可信来源门禁。

旧 command 名称保持不变，避免 WebView、脚本或历史按钮失效；只改变页面归属。

- [ ] **Step 4：运行控制面板测试和前端构建**

```powershell
dotnet test apps/control-panel/ControlPanel.Tests/CodexImSuite.ControlPanel.Tests.csproj
npm --workspace apps/control-panel/web test
npm --workspace apps/control-panel/web run build
```

Expected: 全部 PASS；Memory 页面没有安装、提醒创建或记忆优化主操作。

- [ ] **Step 5：提交 Memory 收口**

```powershell
git add apps/control-panel/web/src/main.tsx apps/control-panel/web/src/styles.css apps/control-panel/Program.cs apps/control-panel/ControlPanel.Tests/MemoryArtifactStoreTests.cs
git commit -m "make memory page an index view"
```

---

### Task 11：兼容迁移、架构文档和 live 验收

**Files:**
- Modify: `docs/PROJECT-ARCHITECTURE.md`
- Modify: `docs/DEVELOPMENT-LOG.md`
- Modify: `README.md` only if user-visible commands or page URLs change
- Modify: `scripts/sync-live-skill.ps1` only if the new CLI is not already copied by directory sync
- Modify: `scripts/test-release-fork-health.ps1` only if it does not fingerprint `skill-lifecycle-cli.mjs`

- [ ] **Step 1：确认迁移和发布健康检查覆盖新产物**

`scripts/shared.ps1` 的 `runtime.dist` 已对整个 runtime `dist` 目录做内容哈希，因此 `skill-lifecycle-cli.mjs` 缺失或内容分叉会让现有 fork health 检查失败，不新增第二套哈希逻辑。先运行以下源产物断言；若失败，回到 Task 4 修复构建入口：

```powershell
$artifact = Resolve-Path -LiteralPath '.\packages\bridge-runtime\dist\skill-lifecycle-cli.mjs' -ErrorAction Stop
$hash = (Get-FileHash -LiteralPath $artifact -Algorithm SHA256).Hash
if ([string]::IsNullOrWhiteSpace($hash)) { throw 'skill-lifecycle-cli.mjs hash is empty' }
Write-Host "skill lifecycle artifact: $hash"
```

Registry 不改写已安装 Skill 由 `skill-registry.test.ts` 覆盖；旧 `extensions` 页面映射由 `panel-navigation.test.ts` 覆盖。若上述已有断言均存在且通过，本步骤不修改发布脚本。

- [ ] **Step 2：运行完整验证，记录任何真实失败**

```powershell
npm --workspace packages/bridge-core test
npm --workspace packages/bridge-runtime test
npm --workspace apps/control-panel/web test
dotnet test apps/control-panel/ControlPanel.Tests/CodexImSuite.ControlPanel.Tests.csproj
powershell -ExecutionPolicy Bypass -File .\scripts\update-architecture-docs.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\build-packages.ps1
```

Expected: 所有测试和构建 exit code `0`；架构检查不再报告未记录的实际边界变化。

- [ ] **Step 3：更新当前架构事实**

`PROJECT-ARCHITECTURE.md` 必须真实记录：

- Registry/Lifecycle 在 runtime，Policy 契约在 core。
- 飞书和面板共用 lifecycle，面板不直接安装 Skill。
- Prompt Snapshot 路径、保留和脱敏规则。
- Memory 只引用 Skill 资产元数据。
- 四域面板和旧路由兼容映射。

`DEVELOPMENT-LOG.md` 将第 10 节状态从“待实施”更新为实际完成范围和剩余风险，不写工具流水。

- [ ] **Step 4：同步 live 并执行可观察验收**

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\sync-live-skill.ps1
powershell -ExecutionPolicy Bypass -File "$HOME\.codex\skills\claude-to-im\scripts\daemon.ps1" restart
powershell -ExecutionPolicy Bypass -File "$HOME\.codex\skills\claude-to-im\scripts\daemon.ps1" status
```

检查：

- `CTI_HOME/runtime/status.json`：`running=true`。
- `CTI_HOME/runtime/bridge-runtime-audit.json`：`feishuWs.state=connected`、`lastUnhandledError=null`。
- `CTI_HOME/logs/bridge.log`：新启动后无 Skill lifecycle 未处理异常。
- 控制面板：四域导航可见，旧地址跳转正确，Prompt 页面有来源和截断信息，Memory 页面只做索引。
- Feishu：已安装 Skill 仍可调用；官方精选未安装会询问；白名单低风险路径记录审计；高风险路径要求 Owner。

- [ ] **Step 5：最终提交**

```powershell
git add README.md docs/PROJECT-ARCHITECTURE.md docs/DEVELOPMENT-LOG.md scripts/sync-live-skill.ps1 scripts/test-release-fork-health.ps1
git status --short
git commit -m "complete registry driven capability governance"
```

提交前必须人工核对 `git status --short`，避免把 release、live skill、WebView2 用户数据、日志、token 或临时 Registry 数据加入 Git。只有用户再次明确要求时才 push 或创建 PR。

---

## 实施时的强制检查点

- 每个 Task 必须先看到新测试按预期失败，再写最小实现。
- 每个 Task 通过目标测试后单独提交；不能累计到最后一次提交。
- 生产代码不得硬编码用户名、机器人名、群 ID、截图路径或本机绝对路径。
- Python、Codex Home、CTI Home、suite root 和来源目录全部通过配置或路径解析器确定。
- 不安装未列入计划的新依赖；若现有依赖无法满足，先报告原因并征得用户确认。
- 不修改 live skill 零散文件；所有运行版变更通过 `sync-live-skill.ps1`。
- 架构事实只在对应代码真正落地后更新，不能把计划写成已实现状态。
